# Agile Diary - 9 May 2026

Date: 9 May 2026

## Activities Completed

- Completed Phase 3 database modelling and SQLAlchemy model implementation
- Finalised the relational database structure using an Entity Relationship Diagram
- Defined primary keys and foreign key relationships between all entities
- Confirmed that no bridge tables were required for the current system design
- Reviewed and validated database normalisation up to Third Normal Form (3NF)
- Created SQLAlchemy models for users, movies, screens, showtimes and bookings
- Implemented relationships between database models using SQLAlchemy relationship() and ForeignKey()
- Added constraints such as nullable=False, unique=True and default values
- Configured automatic table generation using Base.metadata.create_all(bind=engine)
- Verified successful creation of SQLite database tables using DB Browser for SQLite
- Tested backend startup with all database models successfully registered

## Issues Worked On

- #9 Create user database model
- #10 Create movie database model
- #11 Create screen database model
- #12 Create showtime database model
- #13 Create booking database model

## Problems / Blockers

- Encountered SQLAlchemy model mapping errors as used _tablename_ instead of __tablename__ 
- Encountered issues using Python float instead of SQLAlchemy Float for ticket_price
- Needed clarification on foreign key placement and one-to-many relationship design
- Needed to confirm whether bridge tables were required for the current system scope

## Decisions Made

- Decided to use SQLAlchemy models to generate database tables automatically instead of manually creating tables in SQLite
- Decided to use simple integer primary keys named id across all tables for consistency
- Confirmed that foreign keys should only exist on the “many” side of relationships
- Decided not to implement individual seat selection to keep the schema simpler
- Confirmed that no bridge tables were required for the current relational structure

## Next Steps

- Begin creating Pydantic schemas for request and response validation
- Create schemas for authentication, movies, screens, showtimes and bookings
- Prepare the backend structure for API endpoint development
